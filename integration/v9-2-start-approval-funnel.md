# v9.2 Start Approval Funnel Routing

Changes:
- Removed the regular `Get Approved` nav links.
- Removed duplicate approval buttons.
- Kept one approval CTA in the nav:
  `Start Approval → get-approved.html`
- The lead funnel still lives at `/get-approved.html`.
- Existing backend Worker/admin/lead CRM logic is unchanged.
