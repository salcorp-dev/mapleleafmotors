# v12.14 Mobile Category Background Fix

Fix:
- Added a final, stronger mobile/tablet CSS override for the SUV card background image.
- This override appears at the very bottom of styles.css so it wins over older rules.
- Uses a proper car/SUV road image on mobile and desktop.
